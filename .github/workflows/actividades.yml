name: Actividades

on:
  schedule:
    - cron: "0 5,11,17 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Descargar actividades de Intervals.icu
        env:
          ICU_ID: ${{ secrets.INTERVALS_ATHLETE_ID }}
          ICU_KEY: ${{ secrets.INTERVALS_API_KEY }}
        run: |
          python3 - <<'PY'
          import base64, datetime, json, os, sys, urllib.error, urllib.request

          aid = (os.environ.get("ICU_ID") or "").strip()
          key = (os.environ.get("ICU_KEY") or "").strip()

          if not aid or not key:
              sys.exit("ERROR: faltan los secretos INTERVALS_ATHLETE_ID o INTERVALS_API_KEY")
          if aid.isdigit():
              aid = "i" + aid
          print("Atleta:", aid, "| clave de", len(key), "caracteres")

          hoy = datetime.date.today()
          desde = hoy - datetime.timedelta(days=200)
          url = ("https://intervals.icu/
